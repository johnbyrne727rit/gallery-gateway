mutation createPortfolioPeriod($input: PortfolioPeriodInput!, $scholarships: [ID]!) {
  createPortfolioPeriod(input: $input, scholarships: $scholarships) {
    id
    name
    description
    numPieces
    entryStart
    entryEnd
    judgingStart
    judgingEnd
  }
}
