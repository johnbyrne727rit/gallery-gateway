query Scholarship {
  scholarshipByPeriod(periodId: periodId) {
    id,
    scholarships{
      id,
      name,
      gpa,
      yearStatus,
      requiredPhotos,
      description,
      fulltime,
      renewable,
      requiresEssay,
      degreePrograms
    }
  }
}
