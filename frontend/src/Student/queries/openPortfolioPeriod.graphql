query OpenPortfolioPeriod($studentUsername: String!) {
  openPortfolioPeriod(studentUsername: $studentUsername) {
    id
    name
    numPieces
    entryStart
    entryEnd
    scholarships {
      id
      name
      requiredPhotos
    }
  }
}
